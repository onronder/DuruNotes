# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Environment Configuration Files
# DO NOT commit environment files with real secrets to version control
# Keep the template files but ignore the actual configuration files
.env
.env.local
.env.*.local

# Environment files with actual secrets (if using root-level .env files)
/.env.dev
/.env.staging
/.env.prod

# Ignore any environment files in assets if they contain real secrets
# (Note: The template files in assets/env/ should remain in version control)
# assets/env/.env.*
# assets/env/*.local.env

# API Keys and Secrets
api_keys.dart
secrets.dart
config/secrets.json
config/api_keys.json

# Signing configurations
android/key.properties
android/app/upload-keystore.jks
android/app/debug-keystore.jks
ios/Runner/GoogleService-Info.plist.production
ios/Runner/GoogleService-Info.plist.staging

# Build artifacts that might contain embedded secrets
build/
**/build/

# iOS specific signing
ios/Runner.xcodeproj/xcuserdata/
ios/Runner.xcworkspace/xcuserdata/
ios/Pods/
ios/Podfile.lock
ios/Runner/GoogleService-Info.plist.backup

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

sentry.properties
